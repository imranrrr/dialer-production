<%= form_with(model: dialstouser) do |form| %>
  <% if dialstouser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dialstouser.errors.count, "error") %> prohibited this dialstouser from being saved:</h2>

      <ul>
        <% dialstouser.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :dial_id %>
    <%= form.text_field :dial_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
