<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<table class="table table-hover table-bordered">
<thead>
  <tr>
    <th scope="col">First</th>
    <th scope="col">Last</th>
  </tr>
</thead>
<tbody>
  <tr class="field">
    <td><%= form.label :role %></td>
    <% if action_name == 'edit' %>
      <td><%= form.text_field :role, disabled: true %></td>
     <% else %>
        <td><%= form.text_field :role, disabled: false %></td>
     <% end %>  
  </tr>

  <tr class="field">
    <td><%= form.label :role_description %></td>
    <td><%= form.text_area :role_description %></td>
  </tr>
</tbody>
</table>
  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
